repositories {
  jcenter()
  flatDir {
    dirs 'libs'
  }
}

android {
  defaultConfig {
    minSdkVersion 22
    targetSdkVersion 29
  }

  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }

  dependencies {
    implementation(name: 'barcode_scanner_library_v2.6.10.0-release', ext: 'aar')
    implementation(name: 'rfid_scanner_library_2.0.1.34', ext: 'aar')
  }
}
